<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- fondo con patrón -->
    <item>
        <bitmap
            android:src="@drawable/profile_pattern"

            android:gravity="fill" />
    </item>

    <!-- overlay oscuro/claro -->
    <item android:id="@+id/overlay">
        <shape android:shape="rectangle">
            <solid android:color="?attr/colorPatternOverlay"/>
        </shape>
    </item>

</layer-list>
